# Main Server Config
PORT=8001

# Main Server DB (own data — heroes, items, schedule, etc.)
DB_FILE=./data/main_server.db

# Login Server DB (READ-ONLY — only for loginToken validation)
LOGIN_DB_FILE=../login-server/data/super_warrior_z.db

# Server meta
SERVER_ID=1
CURRENCY=USD
SERVER_OPEN_DATE=2024-01-01
