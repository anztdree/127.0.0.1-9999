/**
 * ============================================================================
 *  Recovery — Stamina recovery tick (every 5 minutes)
 * ============================================================================
 */

var logger = require('../utils/logger');

function run() {
    // TODO: implement stamina recovery for all online users
}

function schedule(intervalMs) {
    // TODO: setInterval for recovery tick
}

module.exports = { run: run, schedule: schedule };
